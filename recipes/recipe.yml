# yaml-language-server: $schema=https://schema.blue-build.org/recipe-v1.json
name: lumeos-aurora
description: LumeOS Aurora

# O repositório oficial do Bazzite com KDE para PCs comuns (Desktop/Laptop AMD/Intel)
base-image: ghcr.io/ublue-os/bazzite-kde
image-version: stable

modules:
  - type: files
    files:
      - source: system
        destination: /

  - type: rpm-ostree
    install:
      - google-chrome-stable
      - google-earth-pro-stable
      - spotify-client
      - kdenlive
      - onlyoffice-desktopeditors
      - vlc
      - papirus-icon-theme
    remove:
      - kfind
      - libreoffice-core

  - type: script
    scripts:
      - customizar_lumeos.sh
